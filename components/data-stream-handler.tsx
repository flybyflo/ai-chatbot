"use client";

import { useDataStream } from "./data-stream-provider";

export function DataStreamHandler() {
  const { dataStream } = useDataStream();

  // Data stream handler - artifact functionality removed
  // This component can be used for future stream processing needs

  return null;
}
