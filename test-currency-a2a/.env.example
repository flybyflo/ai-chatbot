# Azure OpenAI Configuration (required)
# These should match your main chatbot's .env.local configuration

# Option 1: Set endpoint directly
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# Option 2: Set resource name (endpoint will be constructed)
# AZURE_RESOURCE_NAME=your-resource-name

# Azure API Key (required)
# AZURE_API_KEY=your_azure_api_key_here

# Azure OpenAI Deployment Name (optional, defaults to gpt-4o-mini)
# AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini

# Optional: Server Configuration
# A2A_HOST=localhost
# A2A_PORT=9999

# NOTE: The agent will automatically load configuration from ../.env.local
# so you don't need to duplicate settings if they're already there!
